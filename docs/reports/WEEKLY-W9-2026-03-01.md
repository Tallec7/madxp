# 📊 Product Weekly — W9 · 2026-02-23 → 2026-03-01

> **Généré automatiquement le** : 18 avril 2026
> **Versions publiées** : v3.76.3 → v3.86.1 (56 releases)
> **PRs mergées** : 19 | **Commits directs** : 105

---

## ÉTAT GLOBAL : 🟡

| Pilier      | État                     | Note                     |
| ----------- | ------------------------ | ------------------------ |
| 🟢 Produit  | 13 feature(s), 5 fix(es) |                          |
| 🟢 Qualité  | Tests stables            | Voir section 4           |
| 🔴 Sécurité | critical: 3, high: 20    | npm audit central-server |
| 🟢 Infra    | CI verte                 | 0 rollback détecté       |

---

## 1. FEATURES

**#432 — Claude/secondary video deployment UI myq kh**

**#430 — feat(ui): add secondary video indicators across dashboard and cloud remote**

**#428 — Claude/multi screen display 1 z9 je**

**#427 — Fix/raspberry variants secondary type**

**#426 — Fix/raspberry variants secondary type**

**#424 — E-22: Rename LED → Secondary Display, fix config-merge regression**

**#423 — Claude/analyze debug page ibqq0**

**#422 — Claude/analyze debug page ibqq0**

**#421 — Claude/install edid decode 9h xt6**

**#417 — Fix unclosed div tag in site-debug-tab template**

**#416 — Claude/debug completeness review kp3 kp**

**#415 — debug: Phase C — restructure dashboard & extract summary bar component**

**#413 — feat(debug): add WiFi client status to debug export bundle**

---

## 2. CORRECTIFS

**#431 — fix(secondary-display): resolve secondary variant for manual video playback**

**#425 — fix(raspberry): rename variants.led → variants.secondary in LoopVideo type** ✅

> - Renames `variants.led` → `variants.secondary` in the `LoopVideo` interface (`sponsor.interface.ts`) — - Fixes the TS2339 build error introduced by commit 953fade1 (LED → Secondary Display rename) where the type wasn't updated to match the code in `tv.component.ts:877-878`

**#420 — fix(debug): make empty buffer status consistent with health score**

**#419 — fix: use adminRateLimit for sponsor routes to prevent 429 cascade**

**#418 — fix(debug): use ReplaySubject to fix pollCommand race condition**

---

## 3. REFACTORISATIONS & PERF

**#414 — refactor: business-first analytics pages with Chart.js integration**

---

## 4. QUALITÉ

### Tests

> ⚠️ Comptes de tests à automatiser — lancer `npm run test:smoke` dans le workflow pour capturer le résultat.

### npm audit — central-server

| Sévérité | Compte |
| -------- | ------ |
| Critical | **3**  |
| High     | **20** |
| Moderate | 2      |
| Low      | 2      |

> 🔴 **Vulnérabilités critiques non traitées — action requise.**

### npm audit — root workspace

| Sévérité | Compte |
| -------- | ------ |
| Critical | 1      |
| High     | 36     |
| Moderate | 9      |
| Low      | 3      |

---

## 5. COMMITS DIRECTS (hors PRs)

**kiosk** (15 commits)

- `a2f0fab` fix(kiosk): re-apply xprop + windowactivate after xrandr in single→dual and failover-return transitions
- `de50f1f` fix(kiosk): invisible 1x1px Chromium window, runtime guard, orphan service detection, docs
- `e6a6b6d` fix(kiosk): suppress Chromium MCS/GCM spam + Pi 5 VSync failures — smoke tests, docs
  _… +12 autres commits_

**debug** (11 commits)

- `495e6b7` feat(debug): ajouter quick-commands Restart Kiosk/App et bouton Apply Services admin
- `2f11461` fix(debug): restore empty buffer status to 'ok' after i18n regression
- `ede2a0a` docs(debug): sync documentation + 3 smoke tests anti-régression i18n
  _… +8 autres commits_

**dual-display** (9 commits)

- `2568bbc` docs(dual-display): ADR-034 v3.89.3 regression guards + doc sync (silent preload, instant reveal, manual→manual)
- `ee878fc` fix(dual-display): cover manual→manual transition gap on slave (ADR-034)
- `7ea4998` fix(dual-display): eliminate slave double-flash on manual video reveal (ADR-034)
  _… +6 autres commits_

**secondary-display** (9 commits)

- `dafc05d` fix(secondary-display): replace F11 with xprop/xdotool for per-monitor fullscreen
- `6cc0f0e` fix(secondary-display): constrain primary Chromium to its monitor in dual-display mode
- `3f55464` docs(secondary-display): add regression tests, troubleshooting guide, and architecture docs
  _… +6 autres commits_

**sync-agent** (5 commits)

- `7a5c801` fix(sync-agent): use actual filename for secondary variant path in config (ADR-033)
- `11e6fd9` fix(sync-agent): restore secondary variants in update_config replace mode (ADR-032)
- `6255fc4` fix(sync-agent): concurrent deploy mutex + startup permission preflight (v3.87.2)
  _… +2 autres commits_

**dashboard** (5 commits)

- `0298c1d` feat(dashboard): secondary display EDID info in debug tab (v3.87.5)
- `82b844c` feat(dashboard): real-time screen resolution display on site detail page (v3.87.4)
- `075d62e` docs(dashboard): documenter affichage EDID enrichi dans page debug
  _… +2 autres commits_

**deploy** (5 commits)

- `838f8a0` fix(deploy): update nginx config during remote deploy (missing /admin/ proxy)
- `b0a1228` fix(deploy): secondary variant enrichment pipeline — defense-in-depth
- `ff2f80a` fix(deploy): auto-repair corrupted /etc/hosts before nginx restart
  _… +2 autres commits_

**network** (4 commits)

- `4cd59b3` fix(network): prevent deploy from killing WiFi dongle via recovery cascade
- `510e442` docs(network): sync all docs with v3.84.9 inter-process scan coordination
- `e1d3edf` fix(network): inter-process wlan1 scan coordination — eliminates dual-scan carrier loss
  _… +1 autres commits_

**analytics** (4 commits)

- `8cd96bd` fix(analytics): harden video-plays INSERT — campaign_id FK pre-check, trigger_type validation, idempotent migration
- `90e4800` docs(analytics): sync documentation et smoke tests anti-regression
- `a21d16a` feat(analytics): refonte business-first des pages analytics fleet et club
  _… +1 autres commits_

**ftp** (4 commits)

- `fd70c01` fix(ftp): add monitoring and regression guards for FTP verification
- `09b78de` fix(ftp): fix verification always failing for files in subdirectories
- `648b2e4` docs(ftp): sync documentation + add smoke tests for FTP ensureDir and display*type guards
  *… +1 autres commits\_

**deployment** (4 commits)

- `9902c23` fix(deployment): distinguish SSH failure (255) from remote script errors in diagnostic
- `dd76e18` fix(deployment): fix post-deployment diagnostic — JSON pollution + silent SSH failure
- `c225a46` fix(deployment): add LIMIT to GET /api/deployments — prevent 500/429 cascade
  _… +1 autres commits_

**hdmi** (4 commits)

- `0dccebc` docs(hdmi): documenter fix sysfs \_findEdidPath + smoke test anti-régression
- `c019065` fix(hdmi): \_findEdidPath utilise readFileSync au lieu de stat.size pour sysfs
- `e874a10` docs(hdmi): documenter enrichissement edid-decode v3.77.0
  _… +1 autres commits_

**tv** (3 commits)

- `8e26373` fix(tv): video cropping on PC — object-fit cover→contain, smoke tests, docs
- `83f78dc` fix(tv): object-fit contain — show full video content without cropping
- `34d8186` fix(tv): viewport overflow on PC browsers — 100vw includes scrollbar width

**admin** (2 commits)

- `b3cacf4` fix(admin): add nginx proxy blocks + HTML-as-JSON defense + demo safety
- `254b996` fix(admin): restore applyServices() lost during build — add to source modules

**failover** (2 commits)

- `8fef4f5` docs(failover): sync documentation with HDMI dual→single fix + add transition monitoring
- `17790ae` fix(failover): relaunch Chromium on HDMI failover instead of resizing in-place

**build** (2 commits)

- `40001a5` fix(build): eliminate 12 Angular build warnings + add smoke guards
- `0681a19` perf(build): optimize Raspberry Pi build output (-49% size, -37% files)

**hotspot-watchdog** (1 commits)

- `3689cdb` fix(hotspot-watchdog): fix false-positive brcmfmac crash detection causing 30s restart loop

**home** (1 commits)

- `9b9411d` feat(home): Angular HomeComponent landing page on neopro.local (US-23.7.2)

**display** (1 commits)

- `8c6fc39` feat(display): resolution detection cascade — auto-detect native TV resolution

**hotspot** (1 commits)

- `6676b1b` fix(hotspot): WiFi boot race — cached scan eliminates RTL8192EU carrier loss

**e-23** (1 commits)

- `44953e3` fix(e-23): finalize remaining gaps — SW audio unlock, analytics source endpoint, docs

**hdmi-resilience** (1 commits)

- `cb32a59` feat(hdmi-resilience): implement Epic E-23 — HDMI resilience & browser access (33 US, 146 SP)

**security** (1 commits)

- `8dcbb7b` fix(security): harden club-config.json permissions and fix nginx diagnostic false positive

**adr** (1 commits)

- `338cf85` docs(adr): add ADR-031 master-slave video loop sync strategy

**diagnostic** (1 commits)

- `301d142` fix(diagnostic): fix false positives in diagnose-pi.sh — nginx PATH, EXT4 parsing, WiFi detection

**session** (1 commits)

- `44c6d7e` docs(session): sync documentation + add regression guards for secondary variant

**ci** (1 commits)

- `e0c7966` fix(ci): add concurrency cancel-in-progress to prevent stale CI runs

**socket** (1 commits)

- `2c07177` fix(socket): guard against null io in cleanup after async delay

**profiles** (1 commits)

- `9e9c542` fix(profiles): fix multi-profile system not working in production (ADR-030)

**smoke** (1 commits)

- `13c4086` fix(smoke): fix startServices regex matching full file instead of fragile function boundaries

**ota** (1 commits)

- `8a1121e` fix(ota): race condition reboot post-OTA — restart sync-agent tuait le timer

**safe** (1 commits)

- `0cb0316` chore(safe): update IMP-PI counter to 31 (edid-decode OTA)

**benchmark** (1 commits)

- `b57a237` fix(benchmark): resolve 500 on /api/benchmark/sites/:siteId — query timeout

---

## 6. DOCS & CHORES

_Aucun._

---

## 7. RISQUES & DÉCISIONS

### Risques semaine prochaine

| Risque                                                                           | Probabilité | Impact                                     | Mitigation                                                                |
| -------------------------------------------------------------------------------- | ----------- | ------------------------------------------ | ------------------------------------------------------------------------- |
| `central-server` : **3 vulnérabilité(s) critique(s)** npm audit non patchées     | Élevée      | Fort (prod exposée)                        | Qualifier avec `npm audit --json` en 1h, patcher ou accepter formellement |
| **19 PR(s)** sans section "Impact client" renseignée                             | Faible      | Faible (rapport incomplet, BO mal informé) | Remplir rétrospectivement ou accepter comme dette de processus            |
| **16 règle(s) de dev** modifiées cette semaine — comportement Claude Code changé | Faible      | Faible (cohérence agents)                  | Relire les diffs en annexe A et valider les nouveaux comportements        |
| **105 commits directs** sur `main` hors PR — revue de code partielle             | Modérée     | Moyen                                      | Passer par des PRs même courtes pour tracer les décisions                 |

### Décisions attendues du BO/DG

1. [SÉCURITÉ — urgent] 3 critical dans `central-server` non ticketées. Bloquer une session pour qualifier et patcher cette semaine ?
2. [PROCESSUS] 19 PR(s) utilisent l'ancien template (pas d'"Impact client"). Remettre à jour ou accepter ?
3. [GOUVERNANCE] 16 règle(s) modifiées (`.claude/rules/services.md`, `.claude/rules/api-routes.md`, `.claude/rules/safe-update.md`, `.claude/rules/security.md`, `.claude/rules/dashboard.md`, `.claude/rules/raspberry.md`, `.claude/rules/testing.md`, `.claude/rules/raspberry-tv.md`, `CLAUDE.md`, `.claude/rules/sponsors.md`, `.claude/rules/saas.md`, `.claude/rules/code-patterns.md`, `.claude/rules/database.md`, `.claude/rules/network.md`, `.claude/rules/ota.md`, `.claude/rules/context.md`). Les nouveaux comportements sont-ils validés par le lead dev ?
4. [DISCIPLINE] 105 commits directs cette semaine. Renforcer la règle PR systématique ou assouplir formellement pour les chores ?

---

---

# ANNEXES

## A. Règles de développement actives

> Ces règles guident le comportement de Claude Code sur ce repo.
> Elles évoluent au fil des sessions — les changements sont trackés ci-dessous.

### Fichiers de règles actifs (16 domaines)

- `.claude/rules/adr.md`
- `.claude/rules/api-routes.md`
- `.claude/rules/code-patterns.md`
- `.claude/rules/context.md`
- `.claude/rules/dashboard.md`
- `.claude/rules/database.md`
- `.claude/rules/network.md`
- `.claude/rules/ota.md`
- `.claude/rules/raspberry-tv.md`
- `.claude/rules/raspberry.md`
- `.claude/rules/saas.md`
- `.claude/rules/safe-update.md`
- `.claude/rules/security.md`
- `.claude/rules/services.md`
- `.claude/rules/sponsors.md`
- `.claude/rules/testing.md`
- `CLAUDE.md` (racine)

### Règles modifiées cette semaine

- `.claude/rules/services.md`
- `.claude/rules/api-routes.md`
- `.claude/rules/safe-update.md`
- `.claude/rules/security.md`
- `.claude/rules/dashboard.md`
- `.claude/rules/raspberry.md`
- `.claude/rules/testing.md`
- `.claude/rules/raspberry-tv.md`
- `CLAUDE.md`
- `.claude/rules/sponsors.md`
- `.claude/rules/saas.md`
- `.claude/rules/code-patterns.md`
- `.claude/rules/database.md`
- `.claude/rules/network.md`
- `.claude/rules/ota.md`
- `.claude/rules/context.md`

<details>
<summary>Diff complet des règles (cliquer pour déplier)</summary>

```diff
diff --git a/.claude/rules/api-routes.md b/.claude/rules/api-routes.md
index 2a43bf4a..e7a9b54a 100644
--- a/.claude/rules/api-routes.md
+++ b/.claude/rules/api-routes.md
@@ -1,12 +1,13 @@
 ---
 paths:
-  - "central-server/src/routes/**"
-  - "central-server/src/controllers/**"
+  - 'central-server/src/routes/**'
+  - 'central-server/src/controllers/**'
 ---

 # API Routes Reference

 ## Auth
+
```

POST /api/auth/login → { email, password } → cookie + user
POST /api/auth/logout → clear cookie
@@ -16,6 +17,7 @@ POST /api/auth/reset-password

```

## Sites (clubs)
+
```

GET /api/sites → liste paginée, filtres: status, sport, region
GET /api/sites/:id → détails + config + metrics
@@ -24,10 +26,12 @@ GET /api/sites/:id/local-content → vidéos locales + stockage
POST /api/sites → créer site (génère api_key)
PUT /api/sites/:id → modifier
DELETE /api/sites/:id → supprimer
+POST /api/sites/:id/copy-config → copier profils config vers un autre site { target_site_id, profile_ids? } (mode ajout)
POST /api/sites/:id/command → envoyer commande au Pi

```

## Debug endpoints (requièrent connexion Pi active)
+
```

GET /api/sites/:id/health-status → santé système
GET /api/sites/:id/diagnostics → diagnostic complet
@@ -38,6 +42,7 @@ POST /api/sites/:id/fix-hotspot → diagnostiquer/réparer le hotspot

```

## Contenu
+
```

POST /api/content/upload → multipart/form-data (vidéo)
GET /api/content/videos → liste vidéos
@@ -47,7 +52,34 @@ POST /api/content/deploy → { videoId, targetType, targetId }
POST /api/content/image-to-video → image → vidéo MP4

````

+### Permissions Club Portal (rôle `club`)
+
+Les utilisateurs `club` ont accès aux endpoints contenu avec des restrictions :
+
+- **Upload** : `uploaded_for_site_id` auto-tagué côté serveur avec `user.site_id`
+- **Liste** : filtrée par `uploaded_for_site_id` = site du club + vidéos MADXP
+- **Delete/Update** : uniquement les vidéos avec `uploaded_for_site_id` = `user.site_id` ET `category ≠ MADXP`
+- **Deploy** : uniquement vers leur propre site
+
+## Video Streaming Proxy (ADR-068)
+
+```
+GET  /api/videos/stream?token=<jwt>  → pipe FTP upstream (Range forwarded)
+```
+
+- Token JWT HS256 court (TTL 1h) signé via `video-token.service.ts`, type = `'video-stream'`.
+- Route mount `app.use('/api/videos', videoStreamRoutes)` **DOIT** précéder `app.use('/api', contentRoutes)` dans `server.ts` — sinon `/videos/:id` capture `/videos/stream` comme `id='stream'` et impose `authenticate` middleware → 401 sur route publique (incident fixé par `c29dda5d` — smoke test enforced).
+- Feature flag `VIDEO_STREAM_PROXY_ENABLED=true` active la signature côté `saas.controller.ts` ; OFF = URLs FTP directes (fallback legacy).
+- Supervision Prometheus : `neopro_video_stream_requests_total{status=success|missing_token|expired|invalid|upstream_error|proxy_error}`.
+
+## Config Save (SaaS direct)
+
+```
+PUT    /api/sites/:id/config            → sauvegarde directe config (SaaS uniquement, refuse Pi)
+```
+
## Config Drafts
+
````

GET /api/sites/:siteId/draft → brouillon du site (ou null)
PUT /api/sites/:siteId/draft → crée/met à jour le brouillon
@@ -57,6 +89,7 @@ POST /api/sites/:siteId/draft/deploy → déploie (vidéos + config orchestr

```

## Subscriptions (v2.47+)
+
```

GET /api/subscriptions/stats
GET /api/subscriptions/at-risk
@@ -68,6 +101,7 @@ POST /api/sites/:id/subscription/reactivate

```

## Alerts & Benchmark
+
```

GET /api/alerts
POST /api/alerts/:id/resolve
@@ -75,14 +109,61 @@ GET /api/benchmark/sites/:siteId
GET /api/benchmark/global

````

+## Pi Analytics (depuis sync-agent — API key optionnelle, piAnalyticsRateLimit 500/min)
+
+```
+POST /api/analytics/video-plays   → { site_id, plays[] } — piAnalyticsRateLimit + authenticateSiteApiKeyOptional
+POST /api/analytics/sessions      → { site_id, action }  — piAnalyticsRateLimit + authenticateSiteApiKeyOptional
+POST /api/analytics/impressions   → { impressions[] }     — piAnalyticsRateLimit + authenticateSiteApiKeyOptional
+```
+
+## Sponsor Portal (PUBLIQUE — magic link token)
+
+```
+GET  /api/sponsor-portal/verify      → ?token=xxx
+GET  /api/sponsor-portal/stats       → ?token=xxx&from=...&to=...
+GET  /api/sponsor-portal/report      → ?token=xxx (PDF)
+GET  /api/sponsor-portal/benchmark   → ?token=xxx
+GET  /api/sponsor-portal/export-csv  → ?token=xxx (CSV)
+```
+
## Remote Cloud (PUBLIQUE - pas d'auth)
+
````

GET /api/remote/:siteId/state → État du site
POST /api/remote/:siteId/command → Envoyer commande
GET /api/remote/:siteId/videos → Vidéos par catégorie

````

+## Remotion Templates (ADR-054 async + ADR-055 versions)
+
+```
+GET    /api/remotion-templates                   → liste (admin voit tout, autres voient `is_published=true`)
+GET    /api/remotion-templates/:id               → détail
+POST   /api/remotion-templates/:id/render        → 202 { job_id } (rendu async — voir /render-jobs/:jobId)
+GET    /api/remotion-templates/render-jobs/:jobId → statut job (status, progress 0-100, phase, video_url, file_size)
+
+# Admin-only (requireRole('admin'|'super_admin')) :
+PATCH  /api/remotion-templates/:id                  → édite name/description/props_schema/default_props
+POST   /api/remotion-templates/:id/duplicate        → clone (unpublished)
+GET    /api/remotion-templates/:id/versions         → historique (DESC, trigger auto-snapshot)
+POST   /api/remotion-templates/:id/versions/:versionId/restore → applique un ancien schéma
+```
+
+**Async render contract (ADR-054)** : `POST /render` ne rend plus la vidéo dans la requête HTTP. Le controller enqueue dans `remotion_render_jobs` et retourne `202 { job_id }`. Le worker in-process (`remotion-render-worker.service.ts`, polling 5s, `FOR UPDATE SKIP LOCKED`) traite les jobs et met à jour `progress` + `phase` (bundling → selecting → rendering → uploading). Le dashboard poll `GET /render-jobs/:jobId`. Supervision : `alerting.checkStuckRenderJobs()` alerte à 15/30 min (warning/critical) et auto-fail à 30 min, + taux échec 1h.
+
+**Versions trigger (ADR-055)** : `trg_neopro_templates_snapshot` capture OLD automatiquement à chaque UPDATE de `props_schema`/`default_props` — la route restore est donc une simple UPDATE qui déclenche elle-même un snapshot (zéro perte possible).
+
+## SaaS (PUBLIQUE — UUID site, ADR-037)
+
+```
+GET  /api/saas/:siteId/config                     → Config complète (URLs vidéo résolues)
+GET  /api/saas/:siteId/profiles                   → Liste des profils disponibles
+GET  /api/saas/:siteId/profiles/:profileId/config → Config d'un profil spécifique
+```
+
## Rate Limiting
+
````

Auth: 60 req/min
Monitoring: 300 req/min (status, metrics, dashboard, benchmark)
@@ -90,8 +171,25 @@ Admin: 400 req/min (lecture sites, logs)
API General: 100 req/min
Sensitive: 30 req/min (commands, deployments)
Remote Cloud: 60 req/min (par IP)
+SaaS: 60 req/min (par IP, remoteRateLimit partagé)
Upload: 10 req/hour
Pi Analytics: 500 req/min (par IP)

```

**Anti-pattern** : ne PAS appliquer `apiRateLimit` globalement ET par route (double comptage).
+
+## NE JAMAIS FAIRE (smoke test enforced)
+
+- Ajouter une route avec paramètre (`:id`, `:siteId`) sans `validateParams(paramSchemas.X)` dans le fichier routes (la validation se fait au niveau routes, pas controllers)
+- Ajouter une route POST/PUT/PATCH avec body sans `validate(schemas.X)` dans le fichier routes
+- Supprimer `authenticateSiteApiKeyOptional` des routes `POST /video-plays` et `POST /sessions` dans `analytics.routes.ts` (sans ce middleware, n'importe quel client peut POST des analytics avec un `site_id` arbitraire)
+- Supprimer `piAnalyticsRateLimit` des routes `POST /video-plays` et `POST /sessions` dans `analytics.routes.ts` (sans rate limiter per-route, les deux routes héritent de `apiRateLimit` 100/min — trop bas p
```

</details>

---

## B. PRs complètes — index

| #                      | Titre                                                        | Type     | Mergé le   |
| ---------------------- | ------------------------------------------------------------ | -------- | ---------- |
| [#432](../../pull/432) | Claude/secondary video deployment UI myq kh                  | feature  | 2026-02-26 |
| [#431](../../pull/431) | fix(secondary-display): resolve secondary variant for manual | fix      | 2026-02-26 |
| [#430](../../pull/430) | feat(ui): add secondary video indicators across dashboard an | feature  | 2026-02-26 |
| [#428](../../pull/428) | Claude/multi screen display 1 z9 je                          | feature  | 2026-02-24 |
| [#427](../../pull/427) | Fix/raspberry variants secondary type                        | feature  | 2026-02-24 |
| [#426](../../pull/426) | Fix/raspberry variants secondary type                        | feature  | 2026-02-24 |
| [#425](../../pull/425) | fix(raspberry): rename variants.led → variants.secondary in  | fix      | 2026-02-24 |
| [#424](../../pull/424) | E-22: Rename LED → Secondary Display, fix config-merge regre | feature  | 2026-02-24 |
| [#423](../../pull/423) | Claude/analyze debug page ibqq0                              | feature  | 2026-02-23 |
| [#422](../../pull/422) | Claude/analyze debug page ibqq0                              | feature  | 2026-02-23 |
| [#421](../../pull/421) | Claude/install edid decode 9h xt6                            | feature  | 2026-02-23 |
| [#420](../../pull/420) | fix(debug): make empty buffer status consistent with health  | fix      | 2026-02-23 |
| [#419](../../pull/419) | fix: use adminRateLimit for sponsor routes to prevent 429 ca | fix      | 2026-02-23 |
| [#418](../../pull/418) | fix(debug): use ReplaySubject to fix pollCommand race condit | fix      | 2026-02-23 |
| [#417](../../pull/417) | Fix unclosed div tag in site-debug-tab template              | feature  | 2026-02-23 |
| [#416](../../pull/416) | Claude/debug completeness review kp3 kp                      | feature  | 2026-02-23 |
| [#415](../../pull/415) | debug: Phase C — restructure dashboard & extract summary bar | feature  | 2026-02-23 |
| [#414](../../pull/414) | refactor: business-first analytics pages with Chart.js integ | refactor | 2026-02-23 |
| [#413](../../pull/413) | feat(debug): add WiFi client status to debug export bundle   | feature  | 2026-02-23 |

---

_Rapport généré par `scripts/generate-weekly-report.mjs` — [GitHub Actions weekly-report.yml](.github/workflows/weekly-report.yml)_
