---
paths:
  - 'central-server/src/controllers/saas*'
  - 'central-server/src/routes/saas*'
  - 'raspberry/src/app/services/saas*'
  - 'raspberry/src/app/services/auth*'
  - 'raspberry/src/environments/**'
  - 'central-dashboard/src/app/features/sites/**'
  - 'raspberry/src/app/components/home/**'
  - 'raspberry/src/app/components/login/**'
---

# SaaS Mode (ADR-037)

Les sites SaaS (`site_type === 'saas'`) fonctionnent en navigateur uniquement, sans Raspberry Pi.

## NE JAMAIS FAIRE

- Supprimer le check `site_type !== 'saas'` dans `saas.controller.ts` (sans ce guard, les endpoints SaaS retournent la config de n'importe quel site Pi/demo — leak de données configs vers des URLs publiques sans auth — smoke test enforced)
- Supprimer `resolveVideoUrl()` / `resolveVideoUrls()` de `saas.controller.ts` (les paths vidéo dans la config sont des chemins relatifs FTP — sans résolution en URLs publiques `https://kalonpartners.bzh/neopro-video/...`, le navigateur SaaS ne peut pas jouer les vidéos — smoke test enforced)
- Supprimer le rate limiting (`remoteRateLimit`) des routes `saas.routes.ts` (endpoints publics sans auth — sans rate limit, un attaquant peut brute-forcer les UUIDs de sites — smoke test enforced)
- Supprimer `validateParams(paramSchemas.siteId)` des routes `saas.routes.ts` (sans validation, un `siteId` malformé passe directement en SQL — smoke test enforced)
- Supprimer `environment.saas.ts` ou la configuration `saas` dans `angular.json` (le build SaaS nécessite les deux — sans eux, `npm run build:saas` échoue silencieusement ou utilise l'environment de prod Pi — smoke test enforced)
- Supprimer le glob `raspberry/public` des assets de la config SaaS dans `angular.json` (sans ce glob, `neopro-logo-white.png`, `favicon.ico`, `manifest.json` et `service-worker.js` sont absents du build SaaS → le splash screen logo request hit le SPA catch-all → 422 + MIME type `text/html` → écran de chargement cassé — smoke test enforced)
- Utiliser `href="/remote"` ou `href="/tv"` dans les templates du projet `raspberry/` (utiliser `routerLink` pour respecter le `baseHref` qui diffère entre Pi `/` et SaaS `/saas/` — smoke test enforced)
- Supprimer la dépendance `deploy-dashboard` du job `deploy-saas` dans `release.yml` (le dashboard `dangerous-clean-slate` supprime `/saas/` — le SaaS doit se déployer APRÈS — smoke test enforced)
- Supprimer la règle `^saas(/.*)?$` du `.htaccess` dashboard (sans elle, le SPA catch-all du dashboard intercepte les requêtes `/saas/` et affiche sa 404 — smoke test enforced)
- Ajouter un environnement Angular Raspberry sans `saasMode` (tous les fichiers `environment*.ts` doivent déclarer `saasMode` — le `HomeComponent` et le `SaasConfigService` lisent ce flag — smoke test enforced)
- Supprimer l'émission `saas-register` du `socket.service.ts` Angular ou le handler `saas-register` du `socket.service.ts` central (sans eux, les sites SaaS n'ont pas de `software_version` dans la DB → le dashboard affiche "N/A" et le monitoring est aveugle — smoke test enforced)
- Afficher `app-connection-indicator` (Hors ligne / uptime / dernière connexion) dans le header pour les sites SaaS (ces métriques reflètent l'agent Pi — le composant doit être gardé par `@if (!isSaas)` dans le header — smoke test enforced)
- Afficher les métriques hardware Pi (CPU, RAM, GPU, ventilateur, HDMI, WiFi) dans l'onglet État pour les sites SaaS (les sites SaaS n'ont pas de matériel — l'onglet État utilise `@if (isSaas)` pour afficher des métriques SaaS dédiées : clients connectés, vidéos jouées, temps écran, sessions — smoke test enforced)
- Supprimer le getter `isSaas` de `site-detail.component.ts` (pilote l'affichage conditionnel État/Debug pour les sites SaaS — sans lui, les sites SaaS affichent des données Pi inexistantes — smoke test enforced)
- Afficher l'onglet Debug pour les sites SaaS dans `site-detail.component.html` (le Debug est Pi-only : GPU, services systemd, fichiers Pi — masqué par `!isSaas` — smoke test enforced)
- Afficher des éléments Pi-only dans les composants enfants de site-detail pour les sites SaaS sans guard `siteType !== 'saas'` : barre "Stockage Pi" / badge "Sur le Pi" / bouton deploy 🚀 / filtres "Sur le Pi"/"À déployer" dans `video-library`, boutons "Supprimer du Pi" dans `video-manager`, section "Pending Deployments" / texte "confirmation du Pi" dans `deployment-status`, warning "Pi hors-ligne" / bannière sync Pi dans `site-profiles-tab`, suffixe ⏳ (pas sur le Pi) dans `loop-manager`, section "Hotspot WiFi" / "Mode Local (Hotspot)" / texte "Deployer (en file)" dans `site-settings-tab`, bouton "Rafraîchir depuis le Pi" / `<app-config-draft>` dans `site-content-tab` — chaque composant enfant reçoit `@Input() siteType` propagé via la chaîne site-detail → site-content-tab → video-manager/deployment-status/config-editor → loop-manager (smoke test enforced)
- Supprimer le guard `saasMode` du constructeur de `HdmiStatusService` (sans ce guard, le service polle `/api/hdmi-status` toutes les 30s sur les sites SaaS → 404 en boucle sur le central server — les sites SaaS n'ont pas de matériel HDMI — smoke test enforced)
- Supprimer le guard `isSaasMode()` de `loadSiteId()` dans tv.component.ts ou appeler `/api/site-info` en mode SaaS (endpoint Pi-local inexistant sur le central — utiliser `SaasConfigService.getSiteId()` en SaaS — smoke test enforced)
- Supprimer l'injection `SaasConfigService` de tv.component.ts (nécessaire pour `loadSiteId()` en mode SaaS — sans elle, le siteId analytics n'est jamais configuré → sponsor analytics perdues — smoke test enforced)
- Supprimer `getSaasClientCount()` de `socket.service.ts` central (compte les navigateurs SaaS connectés en temps réel — utilisé par le dashboard État SaaS — smoke test enforced)
- Compter les onglets `/remote` comme des écrans dans `getSaasClientCount()` (la remote est une télécommande, pas un écran — `emitSaasRegisterIfNeeded()` détecte `/remote` dans le pathname et envoie `clientType: 'saas-remote'` ; `getSaasClientCount` ne compte que `clientType === 'saas-tv'` — smoke test enforced)
- Supprimer le `for (const socketId of room)` de `getSaasClientCount()` ou revenir à `room.size - piInRoom` (l'itération par clientType est le seul moyen d'exclure la remote du comptage — smoke test enforced)
- Oublier `startRecording(false)` et `startSession()` dans `ngOnInit` de tv.component.ts en mode SaaS (sans recording, `trackVideoStart` retourne early silencieusement → 0 video plays ; sans session, `session_id` est null → 0 sessions dans le dashboard — smoke test enforced)
- Appeler `getSaasConfig` ou `getSaasProfileConfig` sans `enrichConfigWithAnalyticsMetadata()` (les vidéos arrivent sur la TV SaaS sans `video_id`, `sponsor_id`, `analytics_category` → les plays sponsors ne sont pas liés aux sponsors en DB → analytics sponsors perdues — smoke test enforced)
- Supprimer `countSessions()`, `countSponsorsDisplayed()` ou `getCompletionRate()` de `analytics.repository.ts` (métriques SaaS utilisées par le dashboard site-detail — smoke test enforced)
- Supprimer `saasMetrics` de la réponse `getSiteDashboardData` dans `site-fleet.controller.ts` (alimente les métriques engagement pour TOUS les types de site — Pi, SaaS, demo — smoke test enforced)
- Re-conditionner le calcul de `saasMetrics` à `site.site_type === 'saas'` seul dans `getSiteDashboardData` (depuis v3.137, les métriques engagement sont calculées pour tous les `site_type` — les re-gater sur SaaS only casse le dashboard club Pi — smoke test enforced)
- Proposer un déploiement OTA sur un site SaaS (les sites SaaS n'ont pas de Raspberry Pi — le sélecteur dashboard utilise `deployableSites` filtré par `site_type !== 'saas'`, et `getTargetSites()` dans `update-deployment.service.ts` exclut les SaaS côté serveur en defense-in-depth — smoke test enforced)
- Envoyer `deploy_video` via `commandQueueService.sendOrQueue()` à un site SaaS (les sites SaaS n'ont pas de Pi — `sendOrQueue` met la commande en queue indéfiniment → déploiement bloqué à 0% → alertes "Déploiement bloqué" en boucle — `deployment.service.ts` doit checker `siteType === 'saas'` et marquer `completed` immédiatement — smoke test enforced)
- Charger `/configuration.json` dans `getConfiguration` resolver de `app.routes.ts` en mode SaaS (ce fichier est local au Pi et n'existe pas sur Hostinger → HTML 404 → JSON parse error → crash-loop config — le resolver DOIT vérifier `saasConfigService.isSaasMode()` AVANT le fallback `/configuration.json` et utiliser `SaasConfigService` pour charger depuis l'API centrale — smoke test enforced)
- Charger `/configuration.json` dans `AuthService.loadConfiguration()` en mode SaaS (même problème — le fichier n'existe pas sur le déploiement SaaS — guard `saasMode` obligatoire au début de `loadConfiguration()` + auto-authenticate `isAuthenticatedSubject.next(true)` car l'auth locale n'existe pas en SaaS — smoke test enforced)
- Utiliser `this.checkAuth()` seul dans `isAuthenticated()` de `auth.service.ts` sans guard SaaS (`checkAuth()` vérifie un token localStorage — en SaaS, il n'y a pas de token → le auth guard redirige vers `/login` → la remote est inaccessible — `isAuthenticated()` DOIT retourner `true` immédiatement quand `saasMode` est actif — smoke test enforced)
- Naviguer vers `/remote` ou `/tv` depuis `home.component.html` sans propager `?site=UUID` en mode SaaS (le siteId est stocké dans localStorage via `SaasConfigService` — si un utilisateur ouvre 2 sites SaaS dans 2 onglets, le dernier écrase le localStorage → les deux onglets affichent le même site — toujours passer `[queryParams]="siteQueryParams"` avec le siteId sur les liens de navigation, `getSiteId()` lit l'URL en priorité — smoke test enforced)
- Appeler `fetch('/configuration.json')` dans `login.component.ts` `loadSiteInfo()` en mode SaaS (retourne du HTML au lieu de JSON → `SyntaxError: Unexpected token '<'` → message "Could not load additional site info" — guard `saasMode` obligatoire — smoke test enforced)
- Remettre le guard `Object.keys(configuration).length === 0` → 404 dans `getSaasConfig` de `saas.controller.ts` (un profil fraîchement créé a une config vide `{}` — retourner 404 casse le chargement SaaS — le contrôleur doit retourner des defaults vides `sponsors: [], categories: []` au lieu de 404 — smoke test enforced)
- Laisser `onVideoDeploy()` dans `site-content-tab.component.ts` sans guard `siteType === 'saas'` (un clic deploy sur un site SaaS envoie `sendOrQueue` qui queue indéfiniment → notification "en file d'attente" trompeuse — le composant DOIT recevoir `@Input() siteType` et return early pour SaaS — smoke test enforced)
- Utiliser `sendCommand('update_config')` ou `confirmDeployLegacy()` pour sauver la config d'un site SaaS (les sites SaaS n'ont pas de Pi — la commande est mise en file d'attente indéfiniment — utiliser `confirmSaveSaas()` qui passe par `mergeDefaultProfileConfig()` → JSONB merge sur le profil par défaut dans `config_profiles` — smoke test enforced)
- Sauver la config SaaS dans `local_config_mirror` au lieu de `config_profiles` (le contrôleur `getSaasConfig` lit `config_profiles` en priorité — si le profil par défaut existe avec `configuration: {}`, il retourne du vide et ne tombe JAMAIS sur `local_config_mirror` — toujours utiliser `mergeDefaultProfileConfig()` qui écrit dans le profil par défaut via JSONB merge `||` — ADR-037, smoke test enforced)
- Afficher "Déployer" / "Déployer (file d'attente)" / "Confirmer le déploiement" dans `deployment-status.component.ts` pour les sites SaaS (utiliser "Enregistrer" / "Enregistrement..." / "Confirmer l'enregistrement" via `common.save` / `common.saving` / `common.confirmSave` — smoke test enforced)
- Afficher le sélecteur merge/replace dans la modal diff pour les sites SaaS (les modes merge/replace sont Pi-only — le `mode-selector` doit être gardé par `siteType !== 'saas'` — smoke test enforced)
- Afficher "En attente de connexion" ou "Dernière connexion" dans la vue État SaaS de `site-detail.component.html` (les sites SaaS n'ont pas de Pi qui se connecte — masquer la version quand `software_version` est absent, utiliser "Dernière session" au lieu de "Dernière connexion", masquer quand `last_seen_at` est null — smoke test enforced)
- Afficher "Sera déployée" (`cloud-hint`) ou le badge ⏳ (`cloud-badge`) dans `loop-manager.component.ts` pour les sites SaaS (les sites SaaS n'ont pas de Pi — les vidéos cloud sont servies directement par URL, aucun déploiement nécessaire — garder par `siteType !== 'saas'` — smoke test enforced)
- Afficher "Deployer" / "Deployer (en file)" dans les boutons overlay et watermark de `site-settings-tab` pour les sites SaaS (utiliser "Enregistrer" via `common.save` — les notifications doivent aussi dire "enregistrée" au lieu de "déployée" ou "en file d'attente" — smoke test enforced)
- Appeler `deployWatermarkAsset()` pour un site SaaS dans `site-settings-tab.component.ts` (les sites SaaS n'ont pas de Pi pour recevoir l'asset — garder par `!this.isSaas` — smoke test enforced)
- Afficher "Modifications non déployées" / "Configuration synchronisée" dans le standalone `config-editor` pour les sites SaaS (utiliser "Modifications non enregistrées" / "Configuration à jour" — le composant doit avoir `@Input() siteType` et un getter `isSaas` — masquer le mode merge/replace avec `*ngIf="!isSaas"` — smoke test enforced)
- Passer un chemin Pi-local (`videos/default/X.mp4`, `videos/sponsors/X.mp4`) directement à `getVideoUrl()` dans `resolveVideoUrl()` de `saas.controller.ts` (le FTP stocke les fichiers à plat à la racine — le préfixe `videos/{category}/` est un chemin de déploiement Pi uniquement — toujours extraire le filename avec `path.split('/').pop()` avant d'appeler `getVideoUrl()` — sinon l'URL résout en 404 HTML → MEDIA_ELEMENT_ERROR → crash loop vidéo SaaS — smoke test enforced)
- Lire la config club uniquement depuis `local_config_mirror` dans le filtre cloud vidéos de `getSiteLocalContent` (pour les sites SaaS, `local_config_mirror` est NULL et la config réelle vit dans `config_profiles` — toujours fallback `configProfileRepository.findDefaultForSite(id)` quand `site.site_type === 'saas'` — sans ce fallback, le filtre club est trop restrictif sur SaaS et masque les vidéos assignées par l'admin via le profil par défaut — smoke test enforced)
- Oublier de propager `[siteType]="siteType"` depuis `club-loop.component.ts` vers `site-content-tab` (sans cette propagation, tous les guards SaaS du composant tombent en mode Pi par défaut → boutons "Déployer", badges "⏳ Sera déployée", section "Hotspot" visibles sur un site SaaS dans le portail club — toujours lire `site.site_type` dans la réponse API et le passer en input — smoke test enforced)
- Hardcoder les labels `'Sur le Pi'` / `'Cloud (à déployer)'` dans `videoOptionGroups` de `site-content-tab.component.ts` (sur les sites SaaS la notion de Pi/déploiement n'existe pas — utiliser `isSaas ? 'Disponibles' : 'Sur le Pi'` et `isSaas ? 'Bibliothèque cloud' : 'Cloud (à déployer)'` — smoke test enforced)
- Servir une config SaaS depuis `getSaasConfig` sans appeler `enrichConfigWithAnalyticsMetadata()` (sans enrichissement, le navigateur TV SaaS reçoit les sponsors sans `video_id`/`advertiser_id`/`analytics_category` → `detectCategory()` tombe en fallback path-based → sponsors classifiés en `'other'` → analytics sponsor SaaS perdues — même règle que pour `update_config` côté Pi — smoke test enforced)
- Lire la config d'un site SaaS uniquement depuis `local_config_mirror` dans `getSiteLocalContent` (pour les sites SaaS, `local_config_mirror` est NULL et la config réelle vit dans `config_profiles` — toujours fallback `configProfileRepository.findDefaultForSite(id)` quand `site.site_type === 'saas'` — sans ce fallback, le filtre vidéos club est trop restrictif sur SaaS et masque les vidéos assignées par l'admin — smoke test enforced)
