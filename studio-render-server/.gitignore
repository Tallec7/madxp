node_modules/
out/
dist/

# Assets binaires lourds (vidéos sources, masks PNG, fonts) — vivent dans
# `studio-template/templates-remotion/public/` côté workspace d'authoring,
# pas dans le repo neopro (5+ GB sinon). Le dev les symlinke via
# `bash scripts/link-assets.sh`. La prod les fetch depuis FTP au boot du
# container (TODO archi cf README).
public/*
!public/.gitkeep

# Studio-poc local renders + photos (ré-générés à la demande)
studio-poc/public/renders/
studio-poc/public/players/
studio-poc/public/logos/

.DS_Store
*.log
