---
phase: 260507-p1a-templates-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - central-server/src/middleware/request-timeout.ts
  - central-server/src/middleware/request-timeout.test.ts
  - central-server/src/middleware/upload.ts
  - central-server/src/routes/remotion-templates.routes.ts
  - central-server/src/services/template-proxy-signing.service.ts
  - central-server/src/services/template-proxy-signing.service.test.ts
  - central-server/src/services/metrics.service.ts
  - central-server/src/controllers/remotion-templates.controller.ts
  - central-server/src/schemas/template-studio.schemas.ts
  - central-dashboard/src/app/features/content/remotion-templates/services/remotion-templates-data.service.ts
  - central-server/src/scripts/rotate-ftp-creds.ts
  - central-server/package.json
  - docs/adr/ADR-113-ftp-creds-rotation-procedure.md
  - docs/adr/README.md
  - central-server/src/__tests__/smoke/smoke-template-uploads-security.test.ts
  - .claude/rules/templates.md
autonomous: true
requirements:
  - AUDIT-P1-7-hmac-proxy-urls
  - AUDIT-P1-8-multer-upload-timeout
  - AUDIT-P0-2-ftp-creds-rotation
must_haves:
  truths:
    - 'Un upload Template Studio qui dépasse 5 minutes retourne 408 Request Timeout (pas un hang silencieux)'
    - 'Une URL proxy /api/remotion-templates/proxy?url=... avec signature HMAC valide + non expirée est servie ; signature invalide ou expirée → 401/403'
    - 'Pendant la phase migration (24h), une URL proxy SANS signature est servie avec log Winston warn + métrique signature_validation{status="missing"}'
    - 'Un super_admin peut suivre la procédure ADR-113 pour roter les FTP creds sans casser la prod (script + doc)'
    - "Si TEMPLATE_PROXY_HMAC_SECRET absente, le serveur fail-fast au boot (pas de fallback silencieux)"
  artifacts:
    - path: 'central-server/src/middleware/request-timeout.ts'
      provides: 'Middleware Express requestTimeout(ms) — applique req.setTimeout + handler 408'
      min_lines: 20
    - path: 'central-server/src/services/template-proxy-signing.service.ts'
      provides: 'signUrl(url, ttlSec) + verifyUrl(url, sig, exp) HMAC-SHA256'
      min_lines: 40
    - path: 'central-server/src/services/template-proxy-signing.service.test.ts'
      provides: '5+ test cases (valid, expired, tampered url, tampered sig, missing secret)'
      min_lines: 50
    - path: 'central-server/src/scripts/rotate-ftp-creds.ts'
      provides: 'Script CLI manuel — procédure step-by-step rotation FTP'
      min_lines: 30
    - path: 'docs/adr/ADR-113-ftp-creds-rotation-procedure.md'
      provides: 'ADR léger format — procédure rotation + cadence 90j'
      min_lines: 40
    - path: 'central-server/src/__tests__/smoke/smoke-template-uploads-security.test.ts'
      provides: '4-6 assertions: timeout middleware appliqué, HMAC verify dans proxy, métrique déclarée, script + ADR existent'
      min_lines: 50
  key_links:
    - from: 'central-server/src/routes/remotion-templates.routes.ts'
      to: 'request-timeout.ts middleware'
      via: 'router.post(\'/upload\', requestTimeout(300_000), uploadAsset, ...)'
      pattern: 'requestTimeout\\(300'
    - from: 'central-server/src/controllers/remotion-templates.controller.ts'
      to: 'template-proxy-signing.service.ts'
      via: 'verifyUrl(url, sig, exp) dans proxyTemplateAsset'
      pattern: 'templateProxySigning\\.verifyUrl|verifyUrl\\('
    - from: 'central-server/src/controllers/remotion-templates.controller.ts'
      to: 'metricsService.recordTemplateProxySignatureValidation'
      via: 'incrément counter selon status valid/invalid/missing'
      pattern: 'recordTemplateProxySignatureValidation'
    - from: 'central-dashboard/.../remotion-templates-data.service.ts'
      to: 'UI message 408'
      via: 'catchError → if status===408 emit "Upload trop long, vérifie ta connexion"'
      pattern: '408|Request Timeout'
---

<objective>
Hardening sécu/réseau Template Studio (audit phase C, 3/4 sub-tasks — CSP unsafe-eval repoussé).

Purpose : Refermer 3 risques latents identifiés par l'audit 2026-05-07 :
- P1 #8 : multer accepte 200 Mo sans timeout → upload qui hang fait tourner la connexion HTTP indéfiniment
- P1 #7 : URLs proxy non signées → DB leak permettrait à un attaquant de forger des fetchs FTP arbitraires
- P0 #2 (reste) : FTP creds non rotables → leak = full write Hostinger sans procédure de rollback

Output : middleware timeout + service HMAC + script rotation + ADR-113 + smoke test enforcing les invariants.
</objective>

<execution_context>
@/Users/gletallec/Documents/NEOPRO/OFFICIEL/neopro/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docs/audits/templates-remotion-audit-2026-05-07.md
@docs/adr/ADR-077-template-studio-preview-and-uploads.md
@docs/adr/ADR-087-no-global-api-rate-limiter-corp-on-429.md
@.claude/rules/templates.md
@central-server/src/middleware/upload.ts
@central-server/src/routes/remotion-templates.routes.ts
@central-server/src/controllers/remotion-templates.controller.ts
@central-server/src/services/metrics.service.ts
@central-server/src/schemas/template-studio.schemas.ts
@central-server/src/config/ftp-storage.ts
@central-dashboard/src/app/features/content/remotion-templates/services/remotion-templates-data.service.ts

<interfaces>
<!-- Conventions imposées par CLAUDE.md + .claude/rules/templates.md -->

Repository pattern strict (CLAUDE.md) :
- Pas de `query()` ou `../config/database` dans controllers
- Tous accès DB via repositories existants

Logger Winston (CLAUDE.md) :
```typescript
import logger from '../utils/logger';
logger.info('Action', { context });
logger.warn('Proxy URL non signée (phase migration)', { url, userId });
logger.error('Proxy signature invalide', { url, reason, userId });
```

Joi validation (CLAUDE.md) — schéma proxy à étendre :
```typescript
// central-server/src/schemas/template-studio.schemas.ts
export const proxyAssetQuerySchema = Joi.object({
  url: Joi.string().uri().required(),
  sig: Joi.string().hex().length(64).optional(),  // HMAC-SHA256 hex = 64 chars
  exp: Joi.number().integer().min(0).optional()
});
```

Métriques Prometheus pattern (cf. ADR-087, ADR-111) :
```typescript
// metrics.service.ts existing pattern
import { Counter } from 'prom-client';
const templateProxySignatureValidation = new Counter({
  name: 'neopro_template_proxy_signature_validation_total',
  help: 'Template proxy URL signature validation results',
  labelNames: ['status'] as const  // 'valid' | 'invalid' | 'missing'
});
```

Middleware existing pattern (cf. middleware/upload.ts) :
```typescript
// Middleware Express signature
import { Request, Response, NextFunction } from 'express';
export function requestTimeout(ms: number) {
  return (req: Request, res: Response, next: NextFunction) => { ... };
}
```

ADR format léger : voir ADR-077, ADR-087, ADR-109.
- Sections : Contexte / Décision / Alternatives rejetées / Conséquences / Fichiers impactés
- Numéro suivant disponible : **ADR-113** (ADR-112 = bodet-led-panel-usb-integration, dernier)

Backward-compat HMAC (24h migration) :
- Phase 1 (cette PR) : verify accepte signature absente avec log warn + métrique status="missing"
- Phase 2 (PR cleanup ultérieure) : drop fallback, signature obligatoire
- Toutes les call-sites qui émettent des proxy URLs doivent être migrés dans cette PR (pour que le warn passe à 0 en prod)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Multer upload timeout middleware + erreur 408 lisible</name>
  <files>
    central-server/src/middleware/request-timeout.ts
    central-server/src/middleware/request-timeout.test.ts
    central-server/src/routes/remotion-templates.routes.ts
    central-dashboard/src/app/features/content/remotion-templates/services/remotion-templates-data.service.ts
  </files>
  <behavior>
    - Test 1 : `requestTimeout(100)` middleware sur route stub — requête qui dort >100ms reçoit 408 + body `{ error: "Request Timeout", code: "REQUEST_TIMEOUT" }`
    - Test 2 : requête rapide (<100ms) passe au handler suivant sans toucher à la response
    - Test 3 : si la response est déjà flushed quand le timeout fire, le middleware ne tente PAS de la re-écrire (guard `res.headersSent`)
    - Test 4 : log Winston warn émis avec `{ method, path, durationMs }` quand le timeout fire
  </behavior>
  <action>
    1. Créer `central-server/src/middleware/request-timeout.ts` :
       - Export `requestTimeout(ms: number)` retournant un middleware Express
       - Utilise `req.setTimeout(ms, () => { ... })` pour armer le timeout côté socket
       - Dans le callback : si `!res.headersSent`, retourner `res.status(408).json({ error: 'Request Timeout', code: 'REQUEST_TIMEOUT', message: 'Upload trop long, vérifie ta connexion' })`
       - Logger Winston warn avec context `{ method: req.method, path: req.path, timeoutMs: ms }`
       - Pas de `console.log` (CLAUDE.md)
    2. Créer `central-server/src/middleware/request-timeout.test.ts` avec les 4 cas du `<behavior>` (utiliser supertest + express minimal app).
    3. Appliquer dans `central-server/src/routes/remotion-templates.routes.ts` :
       - Import `requestTimeout` from '../middleware/request-timeout'
       - Sur route `POST /upload` (super_admin) ET `POST /upload-asset` (authenticated) : `router.post('/upload', requestTimeout(300_000), authenticate, requireSuperAdmin, ...)`
       - **Avant** les middlewares multer pour armer le timeout dès l'ouverture de la connexion
    4. Dashboard `central-dashboard/.../remotion-templates-data.service.ts` :
       - Dans le `uploadAsset` (et autres uploads qui passent par cette route) : `catchError` sur erreur HTTP — si `error.status === 408` : émettre/retourner un message lisible `"Upload trop long, vérifie ta connexion"` (pas un crash silencieux)
       - Ne PAS modifier la signature publique du service (juste enrichir le mapping d'erreur)
    5. Ne modifier AUCUN autre handler upload (out of scope).
  </action>
  <verify>
    <automated>cd central-server && npx jest --testPathPattern='request-timeout' --no-coverage --forceExit</automated>
  </verify>
  <done>
    - 4 tests verts request-timeout.test.ts
    - `grep -n "requestTimeout(300" central-server/src/routes/remotion-templates.routes.ts` retourne au moins 1 occurrence
    - `grep -n "408" central-dashboard/src/app/features/content/remotion-templates/services/remotion-templates-data.service.ts` retourne au moins 1 occurrence
    - Commit : `feat(uploads): add 5min timeout middleware on Template Studio upload routes (audit P1 #8)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HMAC signing service (sign + verify, fail-fast secret)</name>
  <files>
    central-server/src/services/template-proxy-signing.service.ts
    central-server/src/services/template-proxy-signing.service.test.ts
    central-server/src/services/metrics.service.ts
  </files>
  <behavior>
    - Test 1 (valid) : `signUrl('https://kalonpartners.bzh/x.webm', 3600)` retourne `{ url, sig, exp }` ; `verifyUrl(url, sig, exp)` retourne `{ valid: true }` quand exp > now
    - Test 2 (expired) : sig valide mais `exp < Date.now()/1000` → `{ valid: false, reason: 'expired' }`
    - Test 3 (tampered url) : url modifiée après signature → `{ valid: false, reason: 'invalid_signature' }`
    - Test 4 (tampered sig) : sig modifiée → `{ valid: false, reason: 'invalid_signature' }`
    - Test 5 (missing secret) : si `process.env.TEMPLATE_PROXY_HMAC_SECRET` absente, l'import du service throws au boot (fail-fast, pas de fallback `dev-secret` silencieux)
  </behavior>
  <action>
    1. Créer `central-server/src/services/template-proxy-signing.service.ts` :
       - Lecture `const SECRET = process.env.TEMPLATE_PROXY_HMAC_SECRET`
       - Au top-level : `if (!SECRET || SECRET.length < 32) throw new Error('TEMPLATE_PROXY_HMAC_SECRET is required (min 32 chars)')` — fail-fast à l'import
       - `export function signUrl(url: string, ttlSec: number): { url: string; sig: string; exp: number }` : `exp = Math.floor(Date.now()/1000) + ttlSec` ; payload = `${url}|${exp}` ; sig = `crypto.createHmac('sha256', SECRET).update(payload).digest('hex')`
       - `export function verifyUrl(url: string, sig: string | undefined, exp: number | undefined): { valid: boolean; reason?: 'expired' | 'invalid_signature' | 'missing' }` :
         - Si `!sig || !exp` → `{ valid: false, reason: 'missing' }`
         - Si `exp < Date.now()/1000` → `{ valid: false, reason: 'expired' }`
         - Recalcule expectedSig, compare via `crypto.timingSafeEqual` (anti timing attack)
         - Si match → `{ valid: true }`, sinon `{ valid: false, reason: 'invalid_signature' }`
       - Logger Winston `info` au boot du module ("Template proxy signing service initialized") + `error` si verify throw inattendu
       - **Pas de TypeScript `any`** (CLAUDE.md règle stricte)
    2. Créer `central-server/src/services/template-proxy-signing.service.test.ts` avec les 5 cas du `<behavior>`. Pour test 5 : utiliser `jest.isolateModules` pour ré-import après `delete process.env.TEMPLATE_PROXY_HMAC_SECRET`.
    3. Étendre `central-server/src/services/metrics.service.ts` :
       - Ajouter Counter `neopro_template_proxy_signature_validation_total` avec label `status` (`valid|invalid|missing|expired`)
       - Helper `recordTemplateProxySignatureValidation(status: 'valid' | 'invalid' | 'missing' | 'expired'): void`
    4. Note pour Daisy : ajouter `TEMPLATE_PROXY_HMAC_SECRET` aux env vars Railway (manuel, hors-code) — Le smoke test vérifiera qu'en test la var est setée via setupFiles ou .env.test.
  </action>
  <verify>
    <automated>cd central-server && npx jest --testPathPattern='template-proxy-signing' --no-coverage --forceExit</automated>
  </verify>
  <done>
    - 5 tests verts template-proxy-signing.service.test.ts
    - `grep -n "TEMPLATE_PROXY_HMAC_SECRET" central-server/src/services/template-proxy-signing.service.ts` retourne le check fail-fast
    - `grep -n "neopro_template_proxy_signature_validation_total" central-server/src/services/metrics.service.ts` retourne 1 occurrence
    - `grep -n "timingSafeEqual" central-server/src/services/template-proxy-signing.service.ts` retourne 1 occurrence
    - Commit : `feat(security): add HMAC signing service for template proxy URLs (audit P1 #7)`
  </done>
</task>

<task type="auto">
  <name>Task 3: proxyTemplateAsset verify HMAC + sign on emission (migration 24h)</name>
  <files>
    central-server/src/controllers/remotion-templates.controller.ts
    central-server/src/schemas/template-studio.schemas.ts
  </files>
  <action>
    1. Étendre `central-server/src/schemas/template-studio.schemas.ts` :
       - Ajouter `proxyAssetQuerySchema` : `Joi.object({ url: Joi.string().uri().required(), sig: Joi.string().hex().length(64).optional(), exp: Joi.number().integer().min(0).optional() })`
    2. Modifier `central-server/src/controllers/remotion-templates.controller.ts` → fonction `proxyTemplateAsset` :
       - Garder le check `string.includes('kalonpartners.bzh')` existant (defense-in-depth)
       - Importer `templateProxySigningService` (nommer l'import en singleton service-style cohérent avec le repo)
       - Importer `metricsService`
       - Lire `req.query.sig` et `req.query.exp` (validés par Joi en amont)
       - Appeler `verifyUrl(url, sig, exp)` :
         - Si `valid === true` : `metricsService.recordTemplateProxySignatureValidation('valid')` ; servir
         - Si `reason === 'missing'` : **PHASE MIGRATION 24h** — logger Winston `warn('Proxy URL non signée (phase migration ADR-113-bis)', { url, userId: req.user?.id })` + `metricsService.recordTemplateProxySignatureValidation('missing')` + servir quand même (backward-compat)
         - Si `reason === 'expired'` : `recordTemplateProxySignatureValidation('expired')` + 401 `{ error: 'Proxy URL expired' }` + log error
         - Si `reason === 'invalid_signature'` : `recordTemplateProxySignatureValidation('invalid')` + 403 `{ error: 'Proxy URL signature invalid' }` + log error (potentiel signal d'attaque)
       - Préserver les headers CORP/CORS d'ADR-087 sur les réponses 401/403 (Access-Control-Allow-Origin: *, Cross-Origin-Resource-Policy: cross-origin)
    3. Identifier les call-sites qui ÉMETTENT des proxy URLs et les migrer pour signer (TTL 1h = 3600s) :
       - `grep -rn "proxy?url=" central-server/src central-dashboard/src` → lister
       - Chaque call-site qui construit une URL proxy doit appeler `signUrl(originalUrl, 3600)` et concaténer `?url=...&sig=...&exp=...`
       - **Cas attendus** (à confirmer au grep) : controller proxyTemplateAsset n'émet pas (il consomme), mais les fonctions qui retournent `proxy_url` dans les responses API templates (cf. `templateStudioRepository` queries qui transforment des URLs FTP en proxy URLs côté serveur) doivent passer par `signUrl`.
       - Si un call-site est dans le worker `templates-remotion/`, hors scope cette PR — laisser un TODO + log warn (le smoke `missing` capturera la fréquence, on cleanup en PR ultérieure).
    4. Logger Winston `info` au start de proxyTemplateAsset (déjà présent dans le contrôleur — vérifier, sinon ajouter).
    5. **Repository pattern** : aucun `query()` direct dans le controller (CLAUDE.md). La signature/vérification reste dans le service, pas le controller.
  </action>
  <verify>
    <automated>cd central-server && npx jest --testPathPattern='remotion-templates.controller' --no-coverage --forceExit 2>/dev/null || cd central-server && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "(remotion-templates|template-studio.schemas)" || echo "TS clean for touched files"</automated>
  </verify>
  <done>
    - `grep -n "verifyUrl" central-server/src/controllers/remotion-templates.controller.ts` retourne au moins 1 occurrence
    - `grep -n "recordTemplateProxySignatureValidation" central-server/src/controllers/remotion-templates.controller.ts` retourne au moins 3 occurrences (valid/invalid/missing)
    - `grep -n "proxyAssetQuerySchema" central-server/src/schemas/template-studio.schemas.ts` retourne 1 occurrence
    - TypeScript compile sans erreur sur les fichiers touchés
    - Commit : `feat(security): verify HMAC signature on template proxy asset (audit P1 #7, 24h migration window)`
  </done>
</task>

<task type="auto">
  <name>Task 4: FTP creds rotation script + ADR-113</name>
  <files>
    central-server/src/scripts/rotate-ftp-creds.ts
    central-server/package.json
    docs/adr/ADR-113-ftp-creds-rotation-procedure.md
    docs/adr/README.md
  </files>
  <action>
    1. Créer `central-server/src/scripts/rotate-ftp-creds.ts` :
       - Script CLI manuel (pas auto) — le script ne fait QUE printer la procédure step-by-step + valider que les nouvelles env vars passées en argument fonctionnent
       - Steps printés (Winston logger.info) :
         1. "Générer nouveau mot de passe FTP via UI Hostinger (cPanel → FTP Accounts → Edit user → Change password)"
         2. "Tester en local : `FTP_PASSWORD=<new> node -e \"require('./dist/config/ftp-storage').testConnection()\"` (script vérifie la connexion via la config existante)"
         3. "Mettre à jour Railway : `railway variables set FTP_PASSWORD=<new>` (production env)"
         4. "Redeploy Railway : `railway up` ou push commit vide"
         5. "Smoke test post-deploy : `curl https://api.neopro.app/api/health/ftp` (vérifie écriture/lecture FTP test)"
         6. "Archiver l'ancien mot de passe en coffre 1Password (rotation history)"
         7. "Mettre à jour la date de dernière rotation dans `docs/adr/ADR-113-ftp-creds-rotation-procedure.md`"
       - Si le script reçoit `--test-connection <newPassword>` en argument : tente une connexion FTP avec les nouveaux creds et retourne 0/1
       - Aucune modification runtime de la config — c'est un outil de **procédure**
       - Pas de `query()` direct (CLAUDE.md)
    2. Ajouter dans `central-server/package.json` scripts :
       - `"rotate:ftp-creds": "tsx src/scripts/rotate-ftp-creds.ts"`
    3. Créer `docs/adr/ADR-113-ftp-creds-rotation-procedure.md` (format léger, suivre ADR-077/087) :
       - **Date** : 2026-05-07
       - **Statut** : Accepté
       - **Contexte** : `process.env.FTP_PASSWORD` directement utilisé, leak potentiel = full write Hostinger sans procédure de rollback documentée. Audit 2026-05-07 P0 #2 reste.
       - **Décision** :
         - Rotation manuelle tous les **90 jours** (cadence)
         - Procédure step-by-step encodée dans `central-server/src/scripts/rotate-ftp-creds.ts`
         - Commande : `npm run rotate:ftp-creds`
         - Historique des rotations : section "Historique" à la fin de cet ADR (mise à jour à chaque rotation)
       - **Alternatives rejetées** :
         - Rotation automatique CRON : Hostinger ne fournit pas d'API pour changer le mot de passe FTP (manuel uniquement via cPanel)
         - HashiCorp Vault / AWS Secrets Manager : sur-engineering pour la flotte actuelle (1 secret, 1 service)
       - **Conséquences** :
         - Positif : procédure reproductible, traçable, testée
         - Négatif : étape manuelle 90j, ajouter au calendrier ops
       - **Fichiers impactés** : script + cette ADR + section "Historique" — pas de changement runtime
       - **Historique des rotations** : table avec colonnes Date / Opérateur / Hash ancien (sha256 partiel) / Hash nouveau (sha256 partiel) — initialement vide
    4. Mettre à jour `docs/adr/README.md` : ajouter entrée ADR-113 dans la liste/index.
    5. Mettre à jour `.claude/rules/templates.md` (section nouvelle "Sécu uploads/proxy") avec invariants smoke-enforced :
       - "Ne pas retirer `requestTimeout(300_000)` de la route POST /upload"
       - "Ne pas retirer `verifyUrl()` de proxyTemplateAsset"
       - "Ne pas retirer le fail-fast `TEMPLATE_PROXY_HMAC_SECRET` du service"
       - "Ne pas supprimer `npm run rotate:ftp-creds` ni `central-server/src/scripts/rotate-ftp-creds.ts`"
       - Lien vers ADR-113
  </action>
  <verify>
    <automated>test -f central-server/src/scripts/rotate-ftp-creds.ts && test -f docs/adr/ADR-113-ftp-creds-rotation-procedure.md && grep -q "rotate:ftp-creds" central-server/package.json && grep -q "ADR-113" docs/adr/README.md && echo OK</automated>
  </verify>
  <done>
    - 4 fichiers existent et sont cohérents
    - `npm run rotate:ftp-creds --silent --prefix central-server` lance le script (peut sortir 0 sans args, juste print la procédure)
    - ADR-113 référencé dans README ADR
    - .claude/rules/templates.md a les nouveaux invariants
    - Commit : `docs(security): add ADR-113 + rotate-ftp-creds script (audit P0 #2)`
  </done>
</task>

<task type="auto">
  <name>Task 5: Smoke test smoke-template-uploads-security + commit final</name>
  <files>
    central-server/src/__tests__/smoke/smoke-template-uploads-security.test.ts
  </files>
  <action>
    1. Créer `central-server/src/__tests__/smoke/smoke-template-uploads-security.test.ts` avec les assertions suivantes (file-based grep + import check, suivre le pattern de smoke-remotion / smoke-alerts-dedup) :

       **Assertion 1 — Multer timeout** :
       - Lire `central-server/src/routes/remotion-templates.routes.ts`
       - Vérifier qu'il contient `requestTimeout(300_000)` ou `requestTimeout(300000)` sur la route `/upload`
       - Vérifier que `central-server/src/middleware/request-timeout.ts` existe et exporte `requestTimeout`

       **Assertion 2 — Erreur 408 lisible** :
       - Lire `central-server/src/middleware/request-timeout.ts`
       - Vérifier qu'il contient `.status(408)` et `'REQUEST_TIMEOUT'`

       **Assertion 3 — HMAC verify dans proxy** :
       - Lire `central-server/src/controllers/remotion-templates.controller.ts`
       - Vérifier qu'il contient `verifyUrl(` (appel) et l'import du service
       - Vérifier qu'il contient les 3 status `recordTemplateProxySignatureValidation` au moins (valid, invalid, missing)

       **Assertion 4 — Service HMAC fail-fast secret** :
       - Lire `central-server/src/services/template-proxy-signing.service.ts`
       - Vérifier `TEMPLATE_PROXY_HMAC_SECRET` + `throw new Error` (le check fail-fast au boot)
       - Vérifier `timingSafeEqual` (anti timing attack)

       **Assertion 5 — Métrique déclarée** :
       - Lire `central-server/src/services/metrics.service.ts`
       - Vérifier qu'il contient `neopro_template_proxy_signature_validation_total` et `recordTemplateProxySignatureValidation`

       **Assertion 6 — Script + ADR rotation FTP** :
       - Vérifier `fs.existsSync('central-server/src/scripts/rotate-ftp-creds.ts')`
       - Vérifier `fs.existsSync('docs/adr/ADR-113-ftp-creds-rotation-procedure.md')`
       - Lire `central-server/package.json` et vérifier `"rotate:ftp-creds"` dans scripts
       - Lire ADR-113 et vérifier qu'il contient `90` (cadence days) et `Historique des rotations`

       **Assertion 7 — Joi validation proxy query** :
       - Lire `central-server/src/schemas/template-studio.schemas.ts`
       - Vérifier `proxyAssetQuerySchema` exporté avec `sig` et `exp` optionnels

    2. Pattern : utiliser `import * as fs from 'fs'`, `import * as path from 'path'`, et `describe / it / expect` Jest standard.
    3. Le test n'a PAS besoin de booter Express — c'est purement file-based + grep (cohérent avec smoke-remotion).
    4. Ajouter le fichier au matching `npm run test:smoke` (Jest le trouve automatiquement via `__tests__/smoke/smoke-*.test.ts`).
    5. Lancer `npm run test:smoke:smart` une dernière fois pour vérifier toutes les suites pertinentes vertes.
  </action>
  <verify>
    <automated>cd central-server && npx jest --testPathPattern='smoke-template-uploads-security' --no-coverage --forceExit</automated>
  </verify>
  <done>
    - 7 assertions vertes
    - `npm run test:smoke:smart` (depuis racine) vert pour toutes les suites concernées (smoke-template-uploads-security inclus)
    - `npm run test:server` (sample sur les fichiers touchés) vert
    - Story Card produite (CLAUDE.md convention) listant les 3 sub-tasks audit
    - Entrée business changelog `docs/BUSINESS-CHANGELOG.md` semaine en cours bucket 🛡️ Robustesse
    - Commit : `test(smoke): enforce template uploads security invariants (audit phase C)`
  </done>
</task>

</tasks>

<verification>
- All 5 tasks committed atomiquement (Conventional Commits)
- `cd central-server && npx jest --testPathPattern='request-timeout|template-proxy-signing|smoke-template-uploads-security' --no-coverage --forceExit` → all green
- `npm run test:smoke:smart` → green (depuis racine)
- TypeScript compile clean (`cd central-server && npx tsc --noEmit`)
- ADR-113 dans `docs/adr/README.md` et lié depuis `.claude/rules/templates.md`
- `grep -rn "TEMPLATE_PROXY_HMAC_SECRET" central-server/src` retourne 1 fichier (le service uniquement, pas hardcodé ailleurs)
- `grep -rn "FTP_PASSWORD" central-server/src` n'a pas explosé en nouvelles occurrences (juste le script et la config existante)
- Aucun smoke test pré-existant cassé : `npm run test:smoke` complet green avant push
</verification>

<success_criteria>
- Un upload Template Studio qui hang >5min retourne 408 + message lisible côté UI dashboard
- Une URL proxy `/api/remotion-templates/proxy?url=...&sig=...&exp=...` valide est servie ; signature invalide → 403 ; expirée → 401 ; absente → servie + log warn + métrique status="missing" (phase migration 24h)
- Le serveur fail-fast au boot si `TEMPLATE_PROXY_HMAC_SECRET` manquante (pas de fallback silencieux)
- Daisy peut lancer `npm run rotate:ftp-creds` pour suivre la procédure pas à pas
- ADR-113 référencé dans README ADR + .claude/rules/templates.md
- Smoke test enforce les 7 invariants ; toute régression future bloquée en CI
- Métrique Prometheus `neopro_template_proxy_signature_validation_total{status}` observable dans Grafana pour suivre le rolling de la migration
</success_criteria>

<output>
After completion, create `.planning/quick/260507-p1a-templates-s-cu-uploads-timeout-multer-40/260507-p1a-SUMMARY.md` avec :
- Story Card (format CLAUDE.md)
- Diff stats (fichiers / +X / -Y)
- Tests verts (count)
- Risque résiduel : 24h migration window — vérifier `metricsService.neopro_template_proxy_signature_validation_total{status="missing"}` ≈ 0 avant la PR cleanup qui drop le fallback
- Next : (a) PR cleanup pour drop le fallback signature manquante, (b) audit phase C reste = CSP `unsafe-eval` removal (research + Pi visual smoke)
</output>
